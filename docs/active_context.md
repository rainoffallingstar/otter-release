# System Context (Updated: 2026-09-04)

# Current Gate 6 decision banner (2026-09-05)

The accepted Gate 6 comparison scope is complete. Accepted evidence includes the real-Slurm Craftmake–Snakemake executor comparison, corrected Gate A/B preservation, corrected Gate C NM/oracle and CT/GA controls, corrected Gate D complete disagreement accounting, and Methx/Methrix production CpG matrix parity. The planned seven-input fresh legacy-equivalent matrix was **not run** and is not represented as completed evidence; it is now a deferred, non-blocking limitation. WGBS `SRR6373947`, representative `20 samples × 3 repeats`, production-scale/scheduler-pressure testing, and additional Snakemake interruption/recovery work are deferred extensions rather than current Gate 6 blockers. Only the closeout evidence register, decision record, and any conditional BS-PDX publication/artifact verification remain.

## 0. Gate 6 NM semantic correction and corrected-source audit status

源码审计发现 `bamdriver/pkg/bamnative.CalculateNMCheckedWindow` 在 bisulfite 模式下此前无条件同时豁免 `C->T` 与 `G->A`，没有按 SAM `FLAG 0x10` 区分 read strand；`CIGAR X` 也绕过了转换判断。当前工作树已修改为：forward read 仅豁免 `C->T`，reverse read 仅豁免 `G->A`；`M` 和 bisulfite 模式的 `X` 应用相同的链特异转换检查，conventional 模式的 `X` 仍按操作长度计入 NM。

已新增并通过 bamdriver 的 strand-aware NM、`X` CIGAR、独立 `nmoracle` 回归测试，以及 Xenofilx classifier wrapper 回归测试。当前 corrected-source 定向验证已通过：`bamdriver` 的 `pkg/bamnative` 与 `cmd/nmoracle` 测试及 `go vet` 通过；Xenofilx `internal/classifier` 测试及 `go vet` 通过。修复后的两个静态 audit binary 已完成本地构建、SHA-256 记录和远端部署；部署前旧版本已备份。

远端 corrected binary：

- `gate6-nmoracle`：`5b69ef7e55fd258862d01eacf1b359322a79dcb63ac79b482f63877045a33630`
- `gate6-xenofilx-scoreaudit`：`2c797a4425f8837aba39e8c9b374fe4f5ee510e1131e73b5b49a1e891182b9dd`

旧版本备份位于 runtime 下的 `gate6-corrected-backup-20260904/`。RNA 与 BS-PDX corrected read-level audits 已提交：`41965556`（RNA human `SRR1039508`）、`41965558`（BS-PDX host mm10）和 `41965559`（BS-PDX graft hg38）。审计已完成；旧的 `41,892,990` fragment disagreement 仍仅作为历史 baseline，不是当前 corrected-source 结论。

Gate D provenance 路径问题已单独修复并确认：作业 `41964124` 使用冻结 run `run-20260823T034624Z-ndcwfa` 的逻辑路径生成报告。修正后的 membership comparison `41967198` 保留 `1,115,240` 个 disagreements，与该 baseline 完全一致；stratification `41972977` 的 10,000 个确定性样本中，9,192 个为 host-absent，808 个为两参考 complete-pair。score-decision audit `41973407` 完成后显示：9,190 个属于 host-absent score/threshold 差异，808 个属于两参考均映射但 modern 判定 graft-better-total、legacy 因 threshold discarded，另有 2 个双方均 discarded 但原因不同。全量 mapping stratification `41973923` 已成功完成，覆盖全部 `1,115,239` 个 modern-only disagreements：`1,026,316` 个为 host-absent，`88,923` 个为 graft/host 均 complete-pair；全部 `1,115,239` 个 modern 为 complete-pair、legacy 为 none。依赖的全量 score-decision audit `41973925` 也已成功完成，且无 missing score records：`1,025,732` 个为 modern host-absent graft、legacy host-absent discarded，`88,923` 个为 modern graft-better-total、legacy threshold-discarded，`584` 个为双方 discarded 但触发原因不同。三类合计正好覆盖全部 modern-only disagreements，Gate D 的 disagreement accounting 已闭合；bounded legacy/modern semantic difference 已纳入 closeout evidence register，属于记录明确的 scientific limitation，不是未解决的 corrected-source failure。

CpG universe parity 已完成：在同一份冻结 `hg38@GRCh38-gencode-v44` FASTA 上，R/Biostrings `matchPattern("CG")` 的标准染色体结果与 Methx `reference_cpgs.ron` 在统一为 `chr, 0-based start, end-exclusive, strand` 后逐字节一致。两者均为 `29,401,795` 个 CpG，标准化 TSV SHA-256 均为 `553b590409fdefbd392610f06d8444888fda7c65fb0e7c5f1ee3f2e908199f69`，`cmp` exit `0`。这与 Methrix 1.8.1 `extract_CPGs()` 的实现语义一致：对标准染色体执行 `matchPattern("CG")`；但计算节点未安装 `BSgenome.Hsapiens.UCSC.hg38`，因此该证据严格表述为“同一冻结 FASTA 的 Biostrings/Methx RON locus parity”，不把它误写成已直接加载 BSgenome 包的独立 package-to-RON 验收。Methrix GRanges 的默认 strand 表示为 `*`，而 Methx RON 使用 `+`；这是 CpG 位点表示差异，不改变坐标集合。

## 1. 2026-09-03 RNA 人鼠混样 benchmark 状态

已完成 50:50 RNA-seq 混样 pilot：human `SRR1039508` 与 mouse `SRR037954`，两次各 `1,000,000` fragments，STAR 双参考比对，Xenofilx 与 XenofilteR 均使用 conventional NM。4 个双方向分析作业 `41929279`–`41929282` 全部 exit `0`；两个工具在两个 replicate、两个 graft 方向上的 fragment 选择完全一致。human-graft recall 为 `96.7456%`–`96.7656%`，mouse-graft recall 为 `75.5026%`–`75.5986%`。完整证据位于远端 `gate6-human-mouse-rna-mixtures-20260903`。

已完成相同输入、reference、阈值 `6` 和 unmapped penalty `8` 的 `0.60/0.70/0.80/0.90/0.95/0.99` human-fraction 梯度，每个比例两个 replicate，并在 human-graft/mouse-host 与 mouse-graft/human-host 两个方向评估。12 个混样构建作业为 `41938577`–`41938584`、`41938586`–`41938589`，24 个梯度分析作业为 `41941612`–`41941638`（排除未使用 ID）和 `41945110`–`41945113`，均 exit `0`。human-graft recall 全范围约为 `96.76%`，mouse-graft recall 至少到 `95%` human 时仍约 `75.5%–75.7%`，在 `99%` human 时为 `75.43%`；两工具在全部 28 个 cell 的 fragment membership 和 confusion matrix 一致。完整 local TSV、JSON provenance index 与 benchmark brief 已保存在 `xenofilx/benchmark/`。该 RNA 混样 benchmark 是独立的算法/方向性证据；它不替代七输入 fresh matrix、代表性矩阵或 scale gate，但这些项目已按当前 scope decision 明确 deferred，不再作为 Gate 6 closeout blocker。

## 2. 已实现的核心模块 (Modules)

### Reference release builder
- **Path**: `cmd/reference.go`, `internal/reference/build.go`, `internal/reference/build_types.go`
- **Public Methods**:
  - `otter reference build`: stages and atomically publishes a reference release.
  - `reference.BuildRelease(BuildRequest): BuildResult`: copies FASTA/GTF, runs `samtools faidx`, builds selected real Bismark/Bowtie2/STAR indexes, writes typed metadata and verifies publication.
- **Data Flow**: source FASTA + GTF → sibling staging release → FAI + selected indexes → `reference.yaml` + `manifest.json` + `checksums.sha256` → atomic directory rename.
- **Dependencies**: `samtools`, selected index-build executables, shared registry filesystem.
- **Status**: default registry root is `$OTTER_REFERENCE_ROOT`, else `~/.otter/references`; complete releases require at least one real index and are sealed read-only. Gate 6 still requires trusted source provenance and a Paracloud compute-node visibility preflight before any built release can be used. The first bounded mouse build is `mm10-canary@GRCm38-ensembl-100-chr19`, using matching Ensembl release 100 GRCm38 chromosome `19` FASTA and GTF assets filtered to `19`; it is technical-canary-only and cannot stand in for a full `mm10`/`mm38` reference.

### Typed run resolver
- **Path**: `internal/config/v1/`, `internal/config/resolver/`, `internal/run/`
- **Public Methods**:
  - `Resolver.Resolve(ProjectConfig): RunSnapshot` - 将 project/sample/reference/site 固化为 immutable run。
  - `RevalidateSnapshot(RunSnapshot): error` - 在 run/resume 前验证输入、reference 与 workflow assets drift。
- **Data Flow**: `project.yaml + samples.tsv + locks + site` → `runs/<id>/run.yaml`。
- **Dependencies**: reference registry、site profile、workflow asset digest。
- **Status**: FASTQ metadata、resolved resources、required workflow assets 均 fail closed；project/run examples and invalid mutations now execute through both draft 2020-12 JSON Schema and strict Go loaders. Immutable run validation now rejects malformed resolved reference selections and requires provenance for every resolved SLURM field; schema likewise rejects unknown resource/evidence fields and missing SLURM resources. Coverage must still expand to references lock/reference definition schemas and future contract fields.

### SRA production acquisition manifest
- **Path**: `internal/input/sra/manifest.go`, `docs/schema/otter-sra-acquisition-v1.schema.json`
- **Public Methods**:
  - `sra.WriteManifest(SRAAcquisitionManifest): error` - atomically creates an immutable acquisition manifest.
  - `sra.LoadManifest(path): SRAAcquisitionManifest` - strictly decodes and validates provenance.
  - `sra.VerifyManifestFiles(SRAAcquisitionManifest): error` - rechecks regular-file, byte-size, and SHA-256 evidence.
- **Data Flow**: SRA archive + `fasterq-dump` paired FASTQ → checksum/pair audit → `otter.sra-acquisition/v1` → immutable production `run.yaml`.
- **Dependencies**: `sra-tools`, shared Paracloud filesystem, resolved reference releases.
- **Status**: The archive, decoded mate paths, sizes, SHA-256 values, paired-record count, provider MD5, tool command/version, and role-specific reference identities are fail-closed. Publication is create-only and `0444`; verified inputs cannot be retroactively modified. All five currently registered production candidates are `missing_reacquire_for_production` pending a new acquisition record.

### Executor boundary
- **Path**: `cmd/run_craftmake.go`, `cmd/run_snakemake_snapshot.go`, `internal/execution/`, `internal/craftmake/`
- **Public Methods**:
  - `LoadRunInvocation(path, Executor): RunInvocation` - 两个 executor 的统一 immutable snapshot boundary。
  - `craftmake.Execute(Request): Result` - protocol envelope 与 classified exit code。
  - `cmd.ExitCode(error): int` - 向 CLI 与后台 task 保留 Craftmake 分类退出码。
- **Data Flow**: immutable `run.yaml` → explicit executor → local/SLURM backend → task state。
- **Dependencies**: `internal/task/`, Craftmake JSON protocol, Snakemake compatibility config。
- **Status**: Craftmake 默认、Snakemake explicit compatibility；没有 automatic fallback。

### Artifact publication and comparison
- **Path**: `internal/artifact/`, `cmd/artifact.go`
- **Public Methods**:
  - `artifact publish|verify|compare` - immutable manifest 的构建、校验与跨 run 比较。
  - `ComparatorRegistry.Compare(...)` - comparator fail-closed dispatch。
- **Data Flow**: declaration document → stable regular files → checksummed `results/artifacts.json` → verify/compare。
- **Dependencies**: immutable run identity、workflow producer。
- **Status**: expression、PDX count、BAM pair、XLSX workbook structure、HTML、HDF5 signature and typed RNA splicing outcome comparators 已存在。RNA outcome comparison validates the v1 outcome contract and binds declared relative paths to manifest `splicing/files/*` entries while deliberately ignoring run-local absolute `source_root`; individual event files remain exact artifacts. PDX classification comparison now binds each sorted row's ordinal/BAM/BAI names to manifest-declared graft pairs. BAM/BAI comparison rejects symlinks; validates BGZF/BAM magic and reference declarations; and performs bounded streaming validation of each BAI reference's bin, chunk, and linear-index record layout before checking BAM/BAI reference counts. It does not map BAI virtual offsets to BAM records or compare alignment records/bin semantics. XLSX comparison validates required ZIP members and ordered workbook sheet titles, but not cells, formatting, or QC semantics; Methrix remains signature and byte-identity based.

### Workflow producers
- **Path**: `craftmake/workflows/`, `inst/rules/`, `inst/rules_legacy/`
- **Public Methods**: `BeaverBS`, `BeaverRNA`, `BeaverPDX`, `BeaverRNASEQPDX` publish phases。
- **Data Flow**: tool outputs → validated staged files → declaration → `otter artifact publish|verify`。
- **Dependencies**: `otter` CLI、samtools、methx/matsrun/qctb。
- **Status**: All four Craftmake producers and the explicit Snakemake compatibility completion path use results-local temporary staging for producer-owned directories, create-only directory publication, and retry payload tree verification. Snakemake derives declarations from immutable `run.yaml` only after `ExecuteAll()` succeeds, calls the same `artifact.Publish` API, and verifies the resulting manifest. All four `step1` phases, BeaverBS `step2-check` and `step3-check`, BeaverRNA `step2-check`, BeaverPDX `step2-check` and `step3-check`, and BeaverRNASEQPDX `step2-check` and `step3-check` terminate on their real analysis outputs rather than phase-success markers in their Craftmake and workflow-specific Snakemake entries. BeaverBS `step2-check`/`step3-check`, BeaverRNA `step2-check`, BeaverPDX `step2-check`/`step3-check`, and BeaverRNASEQPDX `step2-check`/`step3-check` sample validation now write dimension-aware `otter.sample-artifacts-validation/v1` JSON manifests with regular-file, size, and SHA-256 evidence rather than empty `.ready` files. BeaverPDX and BeaverRNASEQPDX `step2-check` now terminate in `otter.filtered-bam-validation/v1`, which records validated graft filtered BAM/BAI pairs and mapped-read counts; their Craftmake Picard, Xenofilx, filtered-artifact, and phase-success markers were removed. The matching normal Snakemake PDX/RNA-PDX checker entrypoints consume original alignment BAMs and declare Xenofilx as the fixed-name filtered BAM/BAI producer; Picard patch rules are excluded from normal Xenofilx execution and retained only for explicit original-XenofilteR comparison. BS-PDX requires Xenofilx `--bisulfite`; RNA-PDX omits it; both require `--recalculate-nm`. Their `rule all` targets and e2e fixtures no longer depend on patch, filter, or phase-success marker paths. This is static contract coverage only: genuine Snakemake execution, publication, and recovery evidence remain required. BeaverPDX retains GC-bias validation inputs; its `step2-check` terminates in the Xenofilx filtered-BAM validation manifest rather than a MultiQC report. The structured `qctb` `qc_summary.xlsx` remains the separate `step3-check` QC deliverable and does not claim to reproduce MultiQC visualizations. The old step1 checker rules are no longer referenced by those entrypoints; shared checker rules with remaining consumers are retained. RNA paths use `otter.rna-splicing-outcome/v1` (`produced` or `not_applicable`) and stage actual splicing files only for `produced`. Local publish-recovery evidence now covers RRBS, WGBS, RNA-seq, BS-PDX, and RNA-PDX. It includes matching/mismatched partial retries for WGBS and RNA-seq, interruption during BS-PDX staging, interruption after atomic BS-PDX `pdx/graft` publication, interruption after RNA-seq and RNA-PDX `splicing` publication, and interruption after immutable manifest creation; each covered retry verifies either safe completion or existing-manifest idempotency. These are direct Go publisher tests with fake tools, not genuine Snakemake or SLURM execution. All four Craftmake publishers now execute their compiled Bash publish commands through controlled local process interruption: BeaverBS is terminated during staging; BeaverRNA after `splicing` directory publication; BeaverPDX after `methylation` publication; and BeaverRNASEQPDX after `splicing` publication. Each regression verifies the expected partial tree has no manifest, then retries through existing-tree digest comparison, immutable-manifest permissions, and the verify-only branch using fake `otter`/tool commands. This is local compiler-and-shell recovery evidence only; a single all-artifact transaction and real Snakemake/SLURM evidence remain pending.

## 2. Global Types

| Type Name | File Path | Key Fields | 使用场景 |
|---|---|---|---|
| `ProjectConfig` / `RunSnapshot` | `internal/config/v1/types.go` | workflow, execution, paths, references | canonical project/run contract |
| `BuildRequest` / `BuildResult` | `internal/reference/build_types.go` | source FASTA/GTF, registry identity, selected indexes, published paths/digest | immutable reference-release build |
| `ReferenceBuild` configuration | `craftmake/workflows/ReferenceBuild/build.yaml` | source URLs/checksums, allowed contigs, release/tool paths, evidence paths | Gate 6 Slurm reference-build DAG |
| `RunInvocation` | `internal/execution/invocation.go` | snapshot path, project/run/state/results paths | executor-neutral run boundary |
| `SnakemakeArtifactPublicationRequest` | `internal/workflow/snakemake_artifacts.go` | immutable snapshot, snapshot path, samtools/declaration paths | post-success compatibility publication |
| `ExitCodeError` | `internal/craftmake/client.go` | command, classified code, cause | retain Craftmake exit classification |
| `SRAAcquisitionManifest` | `internal/config/v1/types.go` | archive/FASTQ digests, record counts, tool, references | immutable production input provenance |
| `Manifest` / `Declaration` | `internal/artifact/manifest.go` | identity, artifacts, comparison | artifact publication contract |
| `ComparisonReport` | `internal/artifact/comparator.go` | passed, comparable, issues | cross-run artifact comparison |

## 3. API / CLI 注册表

| Command | Input Type | Response Type |
|---|---|---|
| `otter config resolve` | project v1 inputs | immutable `run.yaml` |
| `otter reference build` | FASTA + GTF + build/index options | immutable reference release and digest |
| `otter run` | immutable `run.yaml` | Craftmake or explicit Snakemake task |
| `otter artifact publish` | `run.yaml` + declarations | immutable manifest |
| `otter artifact verify` | `run.yaml` | verification report |
| `otter artifact compare` | left/right `run.yaml` | comparison report |

## 4. Gate 6 Production Validation Update (2026-08-13)

### BAM/NM/classification prerequisite checkpoint (2026-08-26)

- Added `docs/notes/2026-08-26-gate6-bam-nm-classification-parity-plan.md` covering Gate A bamdriver record preservation, Gate B pairbam fragment integrity, Gate C independent Xenofilx reference-aware score validation, and Gate D Picard + XenofilteR fragment-level classification parity.
- Updated `docs/gate6-toolchain-comparison-report.md` and `docs/benchmark-plan.md`: the fresh seven-input modern-vs-legacy matrix is blocked until these four gates pass.
- Code audit confirmed Xenofilx `--recalculate-nm` currently computes an in-memory reference-aware value for classification and does not rewrite output `NM:i`; the future audit must capture score components before BAM writing.
- Discovered controlled remote dependencies: `xenofilx 0.1.0-direct-bamdriver-region-r34`, `pairbam 0.1.0-direct-bamdriver-region-r34`, and `XenofilteR 1.6`. The inherited `picard-3.4.0-0` wrapper is unusable because its Conda Java has a dynamic-link failure and system Java 8 cannot load its Java 17 class files. Created dedicated Enva manifest `enva/src/configs/gate6-picard-java17.yaml` with `openjdk=17` and `picard=3.4.0`; Slurm job `41695075` successfully created the isolated prefix at `/public3/home/scg9946/.local/share/mamba/envs/gate6-picard-java17`. Direct prefix Java reports `17.0.18-internal`, and direct Picard JAR execution reports `Version:3.4.0`; Picard information commands return status 1 by design, so the verification controller accepts status 0 or 1 and matches the expected output. Use the direct prefix Java/JAR invocation for Gate D so inherited `CONDA_PREFIX` cannot select the broken legacy wrapper Java.
- Uploaded `scripts/gate6-bam-nm-baseline-controller.sh` to the isolated Gate 6 runtime and completed readonly real-BAM baseline collection as Slurm job `41694759` (exit `0`, 52m43s). Evidence is under `gate6-bam-nm-parity-20260826-bs-pdx/baseline-41694759`: 90,320,060 mapped primary graft records, 1,885,462 mapped primary host records, 90,220,046 mapped primary modern filtered graft records, complete `NM:i` coverage for all three, and empty `samtools quickcheck` diagnostics.
- Added `enva/src/configs/gate6-picard-java17.yaml` and created the dedicated environment at `/public3/home/scg9946/.local/share/mamba/envs/gate6-picard-java17` through Slurm job `41695075`. Corrected preflight job `41695130` passed with Java `17.0.18-internal`, Picard `3.4.0`, and `SetNmMdAndUqTags`; direct prefix Java/JAR invocation is required for Gate D because the inherited Picard wrapper selects the broken Conda Java.

- Gate C implementation now contains an independently traversed CIGAR/FASTA oracle (`bamdriver/cmd/nmoracle`) and an explicit Xenofilx calculator audit (`xenofilx/cmd/scoreaudit`) whose per-record values are joined by `scripts/gate6-nm-score-compare.py`. Slurm job `41695328` completed in 53 seconds on the first 200,000 mapped records of each original BS-PDX BAM. All 400,000 records had identical identity, recalculated bisulfite NM, insertion/soft-clip components, and classification score; lookup/malformed failures and differences were all zero. This validates the current non-strand-conditioned C-to-T/G-to-A behavior, not a separate strand-aware scientific claim.
- Full Gate A real-BAM no-op round-trip job `41695184` completed successfully in 39m30s. `bamdriver` wrote and quickchecked graft/host round-trip BAMs, then matched decoded headers and ordered canonical records for all 90,320,060 graft and 1,885,462 host records, including auxiliary fields; both comparison reports have `equal=true` and identical record digests. Gate A passes the current no-op preservation contract.
- Gate B pairbam real-data integrity job `41696363` completed successfully. On the frozen mm10 host BAM, pairbam retained all 942,731 complete primary read pairs / 1,885,462 records. The native-name-sort-aware comparator reported `equal=true`, equal retained canonical-record digest, unchanged mate reference/position/TLEN, equal headers, and an empty filtered-name manifest. This is a complete-primary-mates pass; a deliberately mixed incomplete fixture is still desirable for selection-path coverage.
- Gate D fixture preflight passed as Slurm job `41696471`, and the full direct Picard + XenofilteR legacy run completed successfully as job `41696495` in 27m24s: Picard preserved all graft/host input record counts and XenofilteR emitted 6,434,546 filtered graft records. The first dependent membership comparison `41696588` is invalid for acceptance because its modern BAM resolved to prior run `tunvxb` rather than frozen `ndcwfa`; it reported 41,892,990 differences and is retained as a provenance incident. Corrected direct modern Xenofilx job `41696880` and dependent comparison `41696881` used physically identical frozen inputs and completed with 41,892,990 real fragment-membership disagreements. Gate D remains open pending the conventional-NM Xenofilx control and the new per-read Picard/Xenofilx NM audit.
- Gate C per-read NM audit now extends beyond Xenofilx-vs-oracle: `xenofilx/cmd/scoreaudit` emits stored BAM NM plus actual reference-aware NM, and `scripts/gate6-nm-read-compare.py` joins Picard-patched NM against independent conventional/conversion-aware oracle values and Xenofilx by stable alignment identity. `scripts/gate6-nm-read-parity-controller.sh` supports RNA-seq conventional mode and BS-seq mode. BS-seq additionally runs Picard `IS_BISULFITE_SEQUENCE=true` and a strand-split CT/GA-converted-reference control before merging outputs. The BS-PDX host audit was submitted as Slurm job `41712539` and is running on `e0510`; graft and RNA-seq audit submission status remains under confirmation because the remote submission channel is intermittent; no per-read result is claimed yet.

### Current handoff checkpoint (2026-08-26)

- Consolidated comparison report: `docs/gate6-toolchain-comparison-report.md`.
- Formal BS-PDX `SRR36187610` `step3-check` passed after the controller resumed the immutable run with an explicit v2 Methx binary index injected through the controller environment: controller `41687475`, child `41687489`, controller exit `0`.
- Formal output validation passed: five Methx outputs, annotation details rows `7,290,834`, HDF5 `beta`/`cov` shape `(1, 7290833)`, and identical HDF5 SHA-256 `eef88a4de5329de2772cf8f2dacc3fb4b3b64f0c6cb52372cf9d3e930e6d0444`.
- The full isolated BS-PDX benchmark used the staged interval-query binary and v2 index: 7,290,833 CpGs, approximately 3m40s, exit `0`, five outputs, and approximately 9.51 GiB peak RSS.
- The formal catalog was not changed, so no workflow digest drift was introduced. The one-time runtime wrapper is documented in the comparison report and is not a catalog asset.
- Do not claim that this is a complete seven-input modern-vs-legacy scientific comparison. Representative 20-cell × 3-repeat, production scale, WGBS, and remaining publish/parity gates are still open.


- 2026-08-15: Seven decoded inputs are now bound by create-only `otter.sra-acquisition/v1` records (`provenance/otter-sra-acquisition.json`, mode `0444`) under each acquisition root. An isolated comparison runtime at `otter-gate6/toolchain-comparison-20260815T070000Z/runtime` hosts a static Otter, a static current-session Craftmake, and the current workflow catalog whose step1 YAML carries the new legacy QC tasks (`legacy_fastqc_before`, `legacy_fastqc_after`, `legacy_seqkit_statistics`). Legacy tasks use `enva run otter-core -- fastqc|seqkit` because `otter-core` provides FastQC 0.12.1 / SeqKit 2.13.0 on Paracloud. Seven authoritative `otter.run/v1` snapshots were resolved via `otter config resolve --site paracloud-gate6` with per-sample `data/<sample>_R1/_R2.fastq.gz` symlinks to the acquisition-declared `decoded/R1/R2.fastq.gz` files; frozen sample digests equal the acquisition manifests. Step1 plans compile for all seven scenarios through Otter→Craftmake. Slurm controllers `41457463`–`41457468` run `otter run --foreground --executor craftmake --phase step1`; mouse RRBS controller `41457428` already succeeded (`status: succeeded`), producing raw modern/legacy QC, Trim Galore paired outputs, trimmed modern/legacy QC, and SeqKit statistics. Full status in `docs/gate6-paracloud-operations.md` under "Immutable Acquisition Provenance and Otter Snapshots (2026-08-15)".
- The comparison, metric, incident, and promotion contract remains frozen in `docs/gate6-canary-matrix.md` and `docs/benchmark-plan.md`. Executor parity fixes the toolchain; toolchain parity fixes the executor; legacy Perl-versus-Rust Bismark comparison remains explicitly out of scope.
- The accepted current runtime release is `gate6-20260812T104500Z-pdx-host-fasta-r41`. Its Snakemake compatibility projection emits resolved role-specific `reference.graft_fasta` and `reference.host_fasta`; normal Xenofilx consumes those paths directly. BS-PDX retains threshold 6, `--recalculate-nm`, and `--bisulfite`; RNA-PDX retains threshold 4 and `--recalculate-nm` while omitting `--bisulfite`. Accepted Snakemake replays are byte-identical to the corresponding Craftmake BAM/BAI outputs: 97,182 BS-PDX and 118,596 RNA-PDX mapped graft reads.
- 2026-08-20: Xenofilx input queryname-sort memory was hard-coded at 256 MiB, causing 50+ GiB external-sort I/O for the 29.3-GiB hg38 BAM and repeated `step2-check` walltime timeouts (both modern and legacy 12-hour runs ended `TIMEOUT`). Release `0.1.0-direct-bamdriver-region-r35` (xenofilx commit `1184b72`) adds `--sort-memory` (default remains 256 MiB) and is built statically (`CGO_ENABLED=0`) for Paracloud glibc compatibility; it is deployed at `/public3/home/scg9946/.cargo/bin/xenofilx` (old r34 backed up as `xenofilx.r34-backup`). The sealed BeaverPDX catalog now passes `--sort-memory 96G` (craftmake commit `9667551`, compiler suite green) and the remote sealed catalog was updated atomically. Root commit `9e026fc` pins both submodules and the execution note records the diagnosis. New immutable snapshots must be resolved for modern and legacy before rerunning `step2-check` with the optimized binary; full status in `docs/notes/2026-08-20-gate6-bs-pdx-xenofilx-sort-memory-handoff.md`.
- Three balanced Craftmake/Snakemake pairs per PDX scenario completed as real Paracloud Slurm `step2-check` runs. The immutable aggregate-r2 report passed evidence checksum verification, input-content parity, BAM/BAI output checksum parity, and mapped-read parity for all 12 cells. `craftmake/doc/benchmarks/` now retains the verified source report, generated summary CSV, reproducible chart generator, and README-linked SVG. The bounded observation is lower/narrower Craftmake controller reconciliation: BS-PDX median 4.213 s versus 19.819 s; RNA-PDX median 2.651 s versus 9.478 s. With n=3 pairs, the exact two-sided Wilcoxon p-value is 0.250 for each scenario, so the comparison is descriptive rather than a significance or general-throughput claim. The PDX executors use different job topology (Craftmake three workers, Snakemake one); queue delay remains distinct from executor attribution. Representative 20-cell and production-scale gates are still open.
- All five registered candidates (`SRR31480456`, `SRR6373947`, `SRR8397559`, `SRR23802966`, and `SRR30880970`) remain `missing_reacquire_for_production` until an `otter.sra-acquisition/v1` manifest is published. On 2026-08-13, `SRR31480456` was reacquired through the native Go SDL path and its archive identity was independently rechecked: 3,109,362,710 bytes, MD5 `499ec5422c8bad0b6c6ae567e38690bf`, SHA-256 `d501c0360b93ca9f0b5ca02e69d46d71eb998a36d4fbe1b1af65ee619493ab9e`. The strict standalone Craftmake `SRAArchiveDecode/decode` workflow was validated locally and remotely, then completed on Paracloud job `41423708` (`COMPLETED`, `0:0`, 10m36s). It create-only published read-only paired FASTQ outputs. Independent compute-node verifier `41423871` completed (`0:0`, 5m20s), confirmed gzip/`checksums.sha256`, final manifest paths, and `28,865,648` records in each mate: R1 SHA-256 `5050278313b8198185244b02df6cca7d2b4538e65f3da6f971951488690ad6a1`, R2 SHA-256 `2419d75dfcd179d0740b8340dce80bfa0620778adf2654372cba6d70c743dc44`. This is accepted standalone decode execution evidence only until the final `otter.sra-acquisition/v1` provenance record binds these outputs to the RRBS reference role; it must not clear any other candidate.
- On 2026-08-14, the same strict standalone `SRAArchiveDecode/decode` workflow reacquired and decoded the remaining three active archives through fresh create-only Paracloud roots, each verified by an independent compute-node verifier (`gzip -t`, `checksums.sha256`, final-path manifest, record count, SHA-256). RNA-seq `SRR1039508` (Homo sapiens, PRJNA229998) decode `41453199` (`0:0`, 5m43s) and verifier `41453231` (`0:0`, 3m16s) confirmed `22,935,521` records per mate, R1 `1749a8a2902b324d5cd34199d3a8f30bc49c494bfbba6a5395662aa6f3068629`, R2 `02926c7f30d85ec0c68d02142dd16d3282c3c2aa72b585db49bd38cedf782aa7`. RNA-PDX `SRR30880970` (Homo sapiens, PRJNA1168601) decode `41453797` (`0:0`, 17m41s) and verifier `41453991` (`0:0`, 5m36s) confirmed `39,065,482` records per mate, R1 `97dbba27dc30f637d15ab3db1285d47e859d503bb80ca758d40c0301ba2a2aa9`, R2 `c2d1b30f3854da7e0c18b37b806b7cd153e58b9759beeab883daf46306d6be26`. BS-PDX `SRR23802966` (Homo sapiens, PRJNA943199) decode `41453584` (`0:0`, 2h22m07s) and verifier `41454174` (`0:0`, 1h31m13s) confirmed `387,683,390` records per mate, R1 `8b4a489ff4af38c700e9010378ccc84a499373adf3db52f0b164acdf6284983f`, R2 `329f4bf75a90cf11a2415aabcb4b81abc7f9a6bfcc6e50437d5f06cc53d2e2cc`; the BS-PDX decode first failed with `sbatch: Unexpected message received` while the Paracloud Slurmctld was unavailable, and `craftmake resume` re-submitted the same run on recovery. This is accepted standalone decode execution evidence only until final `otter.sra-acquisition/v1` provenance records bind each archive/FASTQ pair to its scenario reference roles.

- R41 Craftmake/Otter compatibility is locally verified: current Craftmake passed the BS-PDX and RNA-PDX `step2-check` compile, Local execution, and cache regressions; Otter passed the r41 Xenofilx filtered BAM/BAI contract suite and its full `go test ./...` regression. No auditable R40 release/configuration is present in the repository or retained local evidence, so no R40 outcome is claimed. The current compatible YAML contract uses `reference.graft_fasta` and `reference.host_fasta`, original step2 graft/host BAM inputs, Xenofilx-generated filtered BAM/BAI names, `--recalculate-nm`, BS-PDX-only `--bisulfite`, and no obsolete step2-check MultiQC dependency.
- `otter.sra-acquisition/v1` now provides a strict, create-only provenance boundary for requalification. Its schema and Go loader require verified archive and paired FASTQ identity, counts, `sra-tools` invocation, and reference-role bindings. The validation matrix keeps PDX replay success while accurately marking full RRBS/WGBS/RNA-seq production validation as blocked on source acquisition.

## 6. Gate 6 Release Update (2026-08-05)

- Immutable r13 passed its initial technical verification but was formally rejected for production because it inherited `enva`, `fastqcx`, and `methx` from an earlier immutable release rather than building every Rust subrepository. `binaries/current` was not changed by r13.
- Accepted r14 release: `gate6-20260805T052000Z-aca58fb-8bb5d64-canonical-qctb-run-config-r14`. Slurm build `41162917` completed on `amd_512`; it native-built `enva`, `fastqcx`, `methx`, and `qctb` using compute-node `rust_build`, `/usr/bin/cc`, `/usr/bin/c++`, shared CMake, `-fuse-ld=bfd`, and the checksum-pinned offline Cargo vendor overlay `dfdc625010443bbc11b92d22e2a562daf3def4c08b306a1dbb2edd9add1e3973`.
- Independent compute-node verifier `41163281` completed. It validated release/source sealing, checksums, all CLI versions, Rust Bismark/Bowtie2, Methx linkage, canonical Craftmake/Snakemake QCTB contracts, absence of PDX QCTB YAML mutation, and rejection of mutable `otter.run/v1` through `qctb --config-dir`.
- `binaries/current` was atomically promoted to r14 only after that independent verification. Fresh paired RRBS Craftmake and explicit Snakemake immutable snapshots remain required before artifact parity or production-matrix claims.

## 5. 待解决的技术债

- [x] Gate 6 RRBS executor-parity reconciliation remediation: immutable r17b release and fresh real-Slurm smoke `41186233`/`41186234` completed with `0:0`; read-only evidence is retained under the r17b evidence root. Under immutable r18, controller interruption/resume, artifact publication interruption/retry, controlled failed-task retry, and RRBS semantic parity all passed. RNA-seq Craftmake snapshot `run-20260806T080001Z-rnaseq` is active on controller `41195609`; its paired Snakemake snapshot `run-20260806T080002Z-rnasnk` is immutable and normalized snapshot parity passed. Controller `41195260` failed before task submission because its invocation omitted required `--phase`; it is a harness-argument incident, not a workflow or scientific result. Next order after RNA-seq is BS-PDX and RNA-PDX paired canaries, then repeat benchmark and representative matrix; WGBS remains deferred.
- [x] Gate 6 RNA-seq Craftmake candidate validation: an isolated immutable candidate snapshot `run-20260806T091001Z-rnacnd` ran all `step1`, `step2`, `step2-check`, and `publish` phases on real Slurm controller `41200713`; every task succeeded and `artifact verify` passed for the sealed four-artifact manifest. This is diagnostic evidence only, not accepted executor-parity evidence: it used candidate Craftmake/Otter binaries while r18 remains immutable. The repair derives `metadata.group_levels` and explicit `seq2mat` `human`/`mouse` species from immutable snapshot samples/references. The paired Snakemake candidate projects `group_levels: 1` and `workflow.species.expression: human` and passes Slurm/DAG dry-run preflight with Snakemake 9.4.1 on PATH; real Snakemake execution awaits a sealed release.
- [ ] Gate 0/1: Expand JSON Schema/Go shared fixtures as project and run contracts gain fields.
- [ ] Gate 5: run genuine Snakemake compatibility publish and interruption/retry evidence on real workflows.
- [x] Gate 6 RNA executor-parity r29: fresh Craftmake and explicit Snakemake controllers completed `step1 → step2 → step2-check → publish`; both manifests individually verified and expression matrix payload checksums matched. The pair remains historical until the normalized-matrix declaration contract is corrected.
- [x] Gate 6 RNA executor-parity r30 release: immutable `gate6-20260808T220000Z-rna-normalized-schema-r30` was built by Slurm job `41299761` (`COMPLETED 0:0`), independently checksum-verified, source-overlay-verified, sealed, and retained as historical release evidence. It records `release.snakemake_normalized_matrix_schema=otter.expression-normalized-matrix/v1`; the normalized RNA expression declaration now matches the Craftmake schema while retaining the shared expression scientific comparator. r31 is the promoted current release.
- [x] Gate 6 RNA executor-parity r31 shared asset fix and fresh pair: immutable `gate6-20260808T233000Z-rna-mutable-snakemake-state-r31` was built by Slurm job `41306089` (`COMPLETED 0:0`), independently checksum-verified, sealed, and promoted to `binaries/current` (`checksums.sha256` SHA-256 `ebdb00ae07d053a63d73551874c41fb5a9e0a001dae6f740de344a59a7afbf3f`). The shared resolver now excludes mutable `.snakemake` runtime directories while retaining regular root `*.snakemake` workflow files; resolver/revalidation regression coverage confirms mutable logs/locks do not invalidate the immutable workflow digest and root workflow files remain covered. Fresh snapshots `run-20260808T233500Z-rabcde` (Craftmake) and `run-20260808T233501Z-rsabcd` (explicit Snakemake) passed paired snapshot parity with identical workflow digest `sha256:4c5fdf8ef6fa8a2ae235dae8a912b02b0abf49d9a40ab79263c55a973be06a63`. Both completed `step1 → step2 → step2-check → publish`; both artifact manifests passed verification, and `otter artifact compare` returned `passed: true`, `comparable: true` for all four declared RNA artifacts. This is the accepted RNA executor-parity evidence. WGBS remains deferred.
- [ ] Gate 5/6 PDX Xenofilx preflight: normal Craftmake and Snakemake PDX paths consume original step2 graft/host BAMs; Xenofilx is the sole filtered BAM/BAI producer with `--recalculate-nm`, while BS-PDX additionally requires `--bisulfite` and RNA-PDX forbids it. Immutable r33 (`gate6-20260809T090043Z-pdx-xenofilx-direct-bam-r33`) was built by `41364171`, independently verified by `41364957`, sealed, and promoted after post-promotion checksum, binary, workflow-contract, and permission validation. Fresh r33 BS-PDX/RNA-PDX snapshots are `run-20260809T103500Z-rabcde` and `run-20260809T103600Z-rfedcb`; BS-PDX Craftmake step1 (`41372800`) and step2 (`41372960`) succeeded, including direct graft/host mapping. BS-PDX step2-check retained two canceled recovery lineages: original `41373394` and retry `41375988`; their completed `sample_artifacts` checks were safely cached. The retry established that MultiQC completed (`41375994`) and Xenofilx was actively computing (4:38 CPU, 3.8 GiB RSS) before deliberate cancellation, so zero-CPU controller accounting is not evidence of an allocation-envelope defect. A stale concurrent resume lineage (`41374558` with child jobs `41374567`/`41374568`) was canceled to protect the shared snapshot state. The single current recovery (`41376752`, Xenofilx child `41376760`) reuses completed checks and MultiQC, executing only Xenofilx. No PDX parity claim is accepted until that recovery, RNA-PDX preflight, and paired executor canaries complete. Local optimization work is intentionally separate from the immutable r33 run: bamdriver now serves indexed 0-based FASTA windows through a shared file descriptor, Xenofilx scores those windows and dispatches complete read-name groups to deterministic bounded workers, and the verified local BS-PDX replay retained byte-identical BAM/BAI output (`fd52b345…706c2` / `cf3cecb4…c534`) while its Xenofilx wall time fell from the prior 272.14 seconds to 108.74 seconds at four workers. This is local performance/semantic evidence only; it does not replace a clean release, reference-aware fixture coverage, or controlled Paracloud acceptance evidence.
- [x] Gate 6 PDX Xenofilx optimized BS-PDX direct-BAM replay: immutable r35 `gate6-20260811T043000Z-pdx-step2-check-assets-r35` was built by `41414094`, independently verified by `41414109`, and promoted by `41414113`. It replaces stale r34 PDX step2-check assets that still scheduled MultiQC; r35 compiles a three-task single-sample DAG consisting of two sample artifact validations and Xenofilx only. Fresh snapshot `run-20260811T110000Z-rxylma` completed the two validation tasks (`41414128`, `41414129`) and optimized Xenofilx (`41414131`) successfully. The Xenofilx-provided filtered BAM/BAI validation manifest records `97182` mapped reads with BAM/BAI SHA-256 `fd52b3456f00eb89fe0e4ca8848530f779fbf49215be464f38537eaa1fb706c2` / `cf3cecb46bf94c2367c6e64473deacd9a4fa072518c9236b2299a136acfdc534`. This is accepted BS-PDX direct-BAM performance/contract evidence for the fresh r35 replay; executor parity, RNA-PDX, semantic comparison, and representative matrix gates remain pending.
- [x] Gate 6 RNA-PDX r35 direct-BAM full replay: fresh immutable snapshot `run-20260811T114500Z-rnaval` completed Craftmake `step1 → step2 → step2-check` under controller `41414723` (`COMPLETED 0:0`). The r35 RNA-PDX step2-check DAG was precompiled as two validation tasks plus Xenofilx, without MultiQC. Its Xenofilx child `41414744` completed successfully with `--recalculate-nm` and no `--bisulfite`; `otter.filtered-bam-validation/v1` records `118596` mapped reads and filtered BAM/BAI SHA-256 `28b8b6e8b93003907d703c49c4255c2b2d567ea3a0cd8266bb1309c20950e83e` / `c0d0d9eff25a39a2bc60cf2e5c22edea7c97311ddf2b09d8e46f7482dcd15ba1`. This accepts the r35 RNA-PDX direct-BAM contract replay. Interruption/recovery evidence, paired explicit-Snakemake executor parity, semantic comparison, and representative-matrix gates remain pending.
- [ ] Gate 5: complete scientific-semantic comparison for Methrix, BAM/BAI, XLSX QC, and splicing outputs before scientific parity claims; current structural checks do not substitute for it.
- [ ] Gate 6: Rust Bismark is globally available through `.cargo/bin` as `bismark`, its compatibility commands, `bowtie2`, and `bowtie2-build`; each link targets immutable `otter-core-bismark-rust-3.1.0-r2`. Compute install `41045847`, global migration `41045990`, and independent validation `41045904` / `41046006` verified its read-only runtime, Enva discovery, global resolution, removal of legacy Perl executables and Conda metadata, Bismark Rust 3.1.0, upstream Bowtie2 2.5.4, supporting Samtools/STAR, and manifest digest `f08d7a63a8b39b55b128111d22c5afa965a1b3b3d84359ea8ed02c2bca4c2f56`. WGBS `SRR6373947` remains deferred. The four active runs (RRBS `SRR31480456`, RNA-seq `SRR1039508`, BS-PDX `SRR23802966`, and RNA-PDX `SRR30880970`) completed native-Go SDL archive acquisition, SDL MD5/size verification, checksum-bound upload, compute-node-only decode, atomic paired `fastq.gz` publication, and independent gzip/SHA-256 validation. BS-PDX paired-count audit job `41040286` verified `387683390` records in both mates. The next required step is deterministic, seed-bound downsampling into immutable canary inputs, followed by real compute-node Craftmake-default and explicit Snakemake compatibility canaries. Only canary semantic parity and real Slurm recovery evidence can unlock the representative 20-cell matrix. The complete `hg19`, `hg38`, canonical `mm10/mm38`, and `mm9` releases are immutable, checksum-verified, read-only, and compute-node-visible. Details, manifest digests, Paracloud interaction conventions, source acquisition procedure, and the future doubled resource policy are recorded in `docs/gate6-paracloud-operations.md`. Local tests are not substitutes.
- [x] Gate 6 Snakemake RRBS `step1` compatibility canary accepted on Paracloud. The immutable accepted release is `gate6-20260803T080841Z-aca58fb-8bb5d64-snakemake-auto-adapter-guard`; its controller lineage includes the self-locating activation fix and the Rust-only runtime policy. The fresh project/snapshot are `gate6-rrbs-20260803-snakemake-auto-adapter-guard` and `run-20260803T081500Z-snkgrd`. Reference resolution now preserves canonical `hg19@GRCh37.p13-gencode-v19` while carrying registry metadata `organism: Homo sapiens`; the Otter-only compatibility adapter projects `species: Homo sapiens`, root `SIDs`, `metadata.sample_ids`, and `directories.qc.before/after` without changing Craftmake. Controller-only dry-run and direct Snakemake DAG preflight resolved exactly four step1 DAG nodes: `fastqcxAtfirst`, `fastq2trim`, `fastqcxAtclean`, and `all`, with no `sbatch` submission. Failed job `41070614` is retained as evidence: resources were correct (`6 CPU`, `8G`, `02:00:00`, `amd_512`, `zc-m6`, `normal`) but the old compatibility rule passed invalid `--adapter AUTO` arguments. The guarded compatibility rule omits adapter flags for `AUTO` and lets Trim Galore auto-detect the Illumina adapter. Accepted job `41071507` completed with `0:0` under exactly `6 CPU`, `8G`, `02:00:00`, `amd_512`, account `zc-m6`, QOS `normal`; all eight expected FastQC/Trim Galore outputs are non-empty, the execution log contains only the three step1 producer rules, no `--adapter AUTO` or `--adapter2 AUTO` invocation remains, and no step2/step3 analysis outputs were produced. This is the accepted bounded Snakemake step1 canary; full-workflow scientific parity and interruption/retry evidence remain pending.
- [ ] Gate 6: Rust Bismark is globally available through `.cargo/bin` as `bismark`, its compatibility commands, `bowtie2`, and `bowtie2-build`; each link targets immutable `otter-core-bismark-rust-3.1.0-r2`. Compute install `41045847`, global migration `41045990`, and independent validation `41045904` / `41046006` verified its read-only runtime, Enva discovery, global resolution, removal of legacy Perl executables and Conda metadata, Bismark Rust 3.1.0, upstream Bowtie2 2.5.4, supporting Samtools/STAR, and manifest digest `f08d7a63a8b39b55b128111d22c5afa965a1b3b3d84359ea8ed02c2bca4c2f56`. WGBS `SRR6373947` remains deferred. The four active runs (RRBS `SRR31480456`, RNA-seq `SRR1039508`, BS-PDX `SRR23802966`, and RNA-PDX `SRR30880970`) completed native-Go SDL archive acquisition, SDL MD5/size verification, checksum-bound upload, compute-node-only decode, atomic paired `fastq.gz` publication, and independent gzip/SHA-256 validation. BS-PDX paired-count audit job `41040286` verified `387683390` records in both mates. The next required step is deterministic, seed-bound downsampling into immutable canary inputs, followed by real compute-node Craftmake-default and explicit Snakemake compatibility canaries. Only canary semantic parity and real Slurm recovery evidence can unlock the representative 20-cell matrix. The complete `hg19`, `hg38`, canonical `mm10/mm38`, and `mm9` releases are immutable, checksum-verified, read-only, and compute-node-visible. Details, manifest digests, Paracloud interaction conventions, source acquisition procedure, and the future doubled resource policy are recorded in `docs/gate6-paracloud-operations.md`. Local tests are not substitutes.
- [x] Gate 6 bounded executor benchmark completed for the active four non-WGBS scenarios on Paracloud using the Rust-only Bismark runtime policy. The accepted benchmark controller release is `gate6-20260803T101602Z-aca58fb-8bb5d64-benchmark-metrics-optional`; it includes generic Craftmake `organism` snapshot compatibility, all-step1 `AUTO` adapter guards, and `otter.executor-benchmark-evidence/v1` with optional Slurm accounting metrics recorded as unavailable when Paracloud omits them. Fresh immutable paired lineages are `gate6-benchmark-rrbs-final-r4` (`run-20260803T101000Z-rbcmaa` / `run-20260803T101001Z-rbsnaa`), `gate6-benchmark-rnaseq-final-r5` (`run-20260803T102000Z-rnacma` / `run-20260803T102001Z-rnasna`), `gate6-benchmark-bs-pdx-final-r5` (`run-20260803T102002Z-bpcmaa` / `run-20260803T102003Z-bpsnaa`), and `gate6-benchmark-rna-pdx-final-r5` (`run-20260803T102004Z-rpcmaa` / `run-20260803T102005Z-rpsnaa`). Create-only benchmark reports are retained at `/public3/home/scg9946/otter-gate6/benchmark-evidence/rrbs-step1-20260803T1010Z.json`, `rnaseq-step1-20260803T1020Z.json`, `bs-pdx-step1-20260803T1020Z.json`, and `rna-pdx-step1-20260803T1020Z.json`. Craftmake and Snakemake step1 allocations completed with `0:0` for all four scenarios: RRBS Craftmake `41075411,41075412,41075439`, Snakemake `41075424`; RNA-seq Craftmake `41075963,41075964,41076001`, Snakemake `41075966`; BS-PDX Craftmake `41076007,41076010,41076063`, Snakemake `41076041`; RNA-PDX Craftmake `41076056,41076058,41076065`, Snakemake `41076274`. Logs show Trim Galore adapter auto-detection and no invalid `--adapter AUTO` invocation in the accepted rule-aligned lineage. A real RRBS Snakemake controller-loss/resume sequence is retained (`41075424`, resumed allocations `41075503,41075555`); the remote background worker boundary interrupted detached workers, while foreground login-node control completed the same immutable snapshot. This is bounded step1 executor benchmark evidence only; full artifact publication/verification, scenario semantic comparison, publication retry, and representative 20-sample × 3-run production gates remain required before promotion.
- [x] Strict benchmark contract hardening passed local tests: workflow asset digest equality and exact one-to-one Slurm `JobIDRaw` matching are enforced. Four create-only strict-pair evidence files were generated under `/public3/home/scg9946/otter-gate6/benchmark-evidence/*-step1-strict-pair-20260804.json`.
- [x] Phase-scoped Craftmake controller identity was added and tested. Craftmake persists `<immutable-run-id>--<phase>` while Otter preserves the snapshot ID at the executor boundary; Snakemake remains explicit and executor-neutral.
- [x] Bismark reference execution-root projection was corrected in both Craftmake and Otter Snakemake adapters: declared `indexes/bismark` resolves to its built `genome` directory. Adapter tests pass.
- [ ] Production blocker: the fresh RRBS correction attempt `run-20260804T015900Z-rbcmia` exposed and retained a temporary double phase-ID release attempt; a fresh release and fresh paired run lineages are still required before rerunning full RRBS step2. No artifact parity or representative matrix has been accepted.
- [x] Gate 6 RRBS r8 retained failure and Methrix repair (2026-08-04): paired r8 completed `step1 → step2 → step2-check → step3` for Craftmake (`run-20260804T062710Z-rbcmnd`) and explicit Snakemake (`run-20260804T062710Z-rbsnld`), but neither `step3-check` completed. The Snakemake checker job `41118172` failed because `methx extract-cpgs` emitted an empty CpG index from the real hg19 reference, whose chromosome sequences are represented as very long unwrapped FASTA records. Craftmake has matching retained checker failure evidence; r8 must not be reused for parity or artifact claims. `methx/src/genome/cpg.rs` now uses streaming FASTA parsing, preserves the prior base across read-buffer boundaries, handles unwrapped sequences and `.gz` input, and has regression coverage for both unwrapped records and a boundary-spanning CpG.
- [x] Paracloud Rust ABI/build pitfall and accepted recovery (2026-08-04): do not publish a locally built `methx` binary to Paracloud—its newer GLIBC requirements make it unloadable on the EL7/GLIBC 2.17 login and compute environment. Also, the default Rust linker (`rust-lld`) in the installed toolchains fails to resolve EL7's `getauxval`, despite the system libc exporting `getauxval@@GLIBC_2.16`. The accepted build procedure is: invoke the native `rust_build` environment binaries explicitly (`$CONDA_PREFIX/bin/cargo`, `$CONDA_PREFIX/bin/rustc`, `$CONDA_PREFIX/bin/rustdoc`); use `/usr/bin/cc` and `/usr/bin/c++`; use the shared CMake 4.0.2 executable at `/public3/home/scg9946/TTest/soft/MyMiniconda/bin/cmake` rather than the environment's CMake 2.8.12; and set `RUSTFLAGS='-C link-arg=-fuse-ld=bfd'` to use the system BFD linker. The resulting dynamically linked binary was verified on Paracloud with a synthetic CpG FASTA and sealed in immutable release `gate6-20260804T080218Z-aca58fb-8bb5d64-methx-streaming-cpg`; `binaries/current` now points there and its `checksums.sha256` verifies. This is platform-native dynamic linking, not a musl/static portability claim.
- [ ] Gate 6 RRBS r10 retained diagnostic lineage (2026-08-04): immutable release `gate6-20260804T091517Z-aca58fb-8bb5d64-rust-bismark-report-memory` removed the unsupported Rust-Bismark nucleotide report input and raised Craftmake Methrix reference extraction to `34G`; fresh paired snapshots are `run-20260804T092209Z-rbcmnd` and `run-20260804T092209Z-rbsnld`. Both executors completed Step1–3 under Slurm, but Step3-check failed in Methrix annotation. Craftmake task `41126159` omitted `--annotation-dir`; the correction supplies `<methylation_call>/methrixh5`. Snakemake checker `41126200` passed that option but its staged directory contained only `reference_cpgs.ron`, with no `hg19.gtf`/`.gtf.gz`; Methrix failed explicitly with `Expected hg19.gtf/.gtf.gz or a detectable *.gtf(.gz)`. The pending r11 correction now explicitly declares the immutable resolved GTF in both Craftmake and Snakemake preparation rules, validates it as a regular file, and stages it as `methrixh5/<graft-species>.gtf`; it no longer guesses from the FASTA directory. Compiler, local integration, static asset, full Otter, and full Craftmake tests pass. A new release, fresh project, and fresh paired snapshots remain mandatory; r10 must not be used for artifact parity. WGBS remains deferred.
- [ ] Gate 6 RRBS r12 retained diagnostic lineage (2026-08-04): release `gate6-20260804T132025Z-aca58fb-8bb5d64-snakemake-qctb-species-sequence` and paired snapshots `run-20260804T132112Z-rbcmaq` / `run-20260804T132112Z-rbsnaq` retained the next executor-specific finding. The explicit Snakemake compatibility path projected `workflow.species.name` as `[hg19]` and successfully produced `results/qc/qc_summary.xlsx`; Craftmake completed prior checker work but did not submit QCTB because its declared `directories.selfconfig/config.yaml` input did not physically exist. The r13 correction preserves `run.yaml` as the sole Craftmake configuration input and now makes QCTB consume that same immutable snapshot directly; QCTB accepts either `--config <run.yaml>` or `--config-dir <run-root>`, where the directory form resolves only `run.yaml`. Craftmake QCTB workflow tasks declare the snapshot file as an input, and QCTB derives its sample IDs, role-aware graft/primary reference, scenario mode, and run-local QC paths from `otter.run/v1`; temporary PDX YAML rewrites are removed. Snakemake retains its state-local compatibility YAML only for its legacy executor; its QCTB rules pass `directories.qctb_config`, which resolves to the immutable `run.yaml`. QCTB rejects mutable snapshots; the legacy/nested YAML loader remains compatibility-only. Full Otter, full Craftmake, focused Craftmake adapter/compiler, canonical RRBS QCTB plan compilation, and QCTB Rust test suites pass. Craftmake CLI integration tests now build one package-scoped shared binary and copy it into each isolated fixture, avoiding repeated concurrent `go build` cache contention. New immutable r13 release/project/snapshots and real paired Slurm execution are still required; no r12 results may be modified or reused for artifact parity.
