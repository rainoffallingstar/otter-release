% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/beaver_enrichr.R
\name{beaver_enrichr}
\alias{beaver_enrichr}
\title{Perform EnrichR analysis using ggetrs and visualize results}
\usage{
beaver_enrichr(
  pathwaydb = "KEGG_2021_Human",
  genes,
  output = NULL,
  filter_p = 0.05,
  plot = T,
  top = 20
)
}
\arguments{
\item{pathwaydb}{Character. The name of the pathway database to use (e.g., "KEGG_2021_Human"). Default is "KEGG_2021_Human".}

\item{genes}{Character vector. The list of gene symbols to be analyzed.}

\item{output}{Character. The directory or file path to save the enrichment results. If NULL, a default directory "enrichr" will be created. Default is NULL.}

\item{filter_p}{Numeric. The p-value threshold for filtering significant pathways. Default is 0.05.}

\item{plot}{Logical. Whether to generate a dot plot of the top enriched pathways. Default is TRUE.}

\item{top}{Integer. The number of top pathways to display in the plot. Default is 20.}
}
\value{
A data frame containing the filtered enrichment results, including pathway names, p-values, and overlapping genes.
}
\description{
This function performs gene set enrichment analysis using the EnrichR API via the \code{ggetrs} Rust tool.
It queries the specified pathway database, filters results by p-value, and optionally plots the top enriched pathways.
The results are saved in a YAML file and a PDF plot (if requested).
}
\details{
This function requires the \code{ggetrs} Rust tool to be installed and accessible via the \code{cargo} binary.
The function will stop if \code{ggetrs} is not found. The results are saved in a YAML file and optionally plotted using \code{ggpubr}.
}
