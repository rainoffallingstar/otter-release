% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conflict_network_analysis.R
\name{conflict_network_analysis}
\alias{conflict_network_analysis}
\title{Perform Conflict Network Analysis for Pathway-Specific Methylation}
\usage{
conflict_network_analysis(df, pathway, edgelist, graph_layout = "graphopt")
}
\arguments{
\item{df}{Data frame. The input data frame containing methylation results.
Expected columns include "Term" (or "term_name") and "Genes" (or "overlapping_genes").}

\item{pathway}{Character vector. The pathway term(s) to filter the genes.}

\item{edgelist}{Data frame. The edge list of the pathway's gene network.
Expected columns include "from" and "to" for gene interactions.}

\item{graph_layout}{Character. The layout algorithm for plotting the network. Default is "graphopt".}
}
\value{
A list containing:
\itemize{
\item \code{overlap}: A list with hyper-, hypo-, and intersecting genes.
\item \code{graph}: The igraph object representing the gene network.
\item \code{ggraph}: The ggraph plot object of the network.
}
}
\description{
This function analyzes the conflict between hyper- and hypo-methylated genes within a specified pathway,
constructs a gene network based on the pathway's edge list, and visualizes the network with node annotations.
}
\details{
The function performs the following steps:
\enumerate{
\item Identifies hyper- and hypo-methylated genes from the input data frame.
\item Filters genes based on the specified pathway.
\item Maps genes to their Entrez IDs and annotates them with methylation direction (hyper, hypo, or bidirectional).
\item Constructs a subgraph of the pathway's gene network using the filtered genes.
\item Visualizes the network with nodes colored by methylation direction and labeled with gene symbols.
}

This function assumes that the input data frame \code{df} contains columns for pathway terms and gene lists.
The \code{edgelist} should be a data frame representing the gene interactions within the pathway.
}
