% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/beaverflow_install.R
\name{beaverflow_install}
\alias{beaverflow_install}
\title{Install and Configure the Beaverflow Workflow Environment}
\usage{
beaverflow_install(
  workflow_dir = getwd(),
  enigent = beaver_engine_detect(),
  build_env = F,
  build_genome = F
)
}
\arguments{
\item{workflow_dir}{The directory where the Beaverflow workflow environment will be installed.
Defaults to the current working directory (\code{getwd()}).}

\item{enigent}{The detected engine type. Can be one of "k8s", "container", or other values detected by \code{beaver_engine_detect()}.
Defaults to the result of \code{beaver_engine_detect()}.}

\item{build_env}{install conda envs in the init of the beaverflow, default as False}

\item{build_genome}{download and compile genome files in the init of the beaverflow, default as False}
}
\description{
This function sets up the Beaverflow workflow environment by creating the necessary directory structure,
fetching runtime files, installing required software packages, and configuring the environment.
It supports different installation modes based on the detected engine (e.g., Docker, local) and user input.
}
\details{
The function performs the following steps:
\enumerate{
\item Creates a directory structure under \code{workflow_dir} with folders such as "config", "data", "envs", etc.
\item Fetches runtime files (e.g., Snakefile) and R scripts from the \code{xdxtools} package.
\item Installs required software packages using Conda, including Python environments and R packages.
\item Configures the environment based on the detected engine type and user input.
}

If the detected engine is "k8s" or "container", it assumes a pre-built Docker image is used and prompts the user to confirm.
If not, it installs Conda environments and R packages locally.

Note that this function requires an active internet connection to download and install packages.
Additionally, the user may need to download pre-built genomic files from a shared link and place them in the "inst/" directory.
}
