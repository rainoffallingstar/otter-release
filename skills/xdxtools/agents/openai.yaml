interface:
  display_name: "xdxtools Workspace"
  short_description: "Work on xdxtools and its matched subrepos"
  default_prompt: "Use $xdxtools to work in the xdxtools repo or one of its matching submodules with the right toolchain and validation flow."

policy:
  allow_implicit_invocation: true
